image: registry.gitlab.dev-o-sud.fr/public-devosud/docker-images/nodejs

cache:
  untracked: true
  paths:
    - node_modules/

stages:
  - build
  - test
  - deploy


build:
  stage: build
  script:
    - npm install
    - ng build --prod
  tags:
    - nodeJS

test:
  stage: test
  script:
    - ng test
  tags:
    - nodeJS

deploy:
  stage: deploy
  script:
    - netlify deploy --site $NETLIFY_SITE_ID --auth $NETLIFY_AUTH_TOKEN --prod
  only:
    - master
  tags:
    - nodeJS
